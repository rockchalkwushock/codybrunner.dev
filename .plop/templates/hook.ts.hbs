// import * as React from 'react'
// import { useQuery, UseQueryResult } from 'react-query'

// import {} from '@interfaces/{{ camelCase serviceName }}'
// import {} from '@lib/{{ camelCase serviceName }}'

type {{ properCase hookName }} = () => void

export const {{ camelCase hookName }}: {{ properCase hookName }} = () => {
  // return useQuery('{{ dashCase endpointName }}', async () => {}, {})
  return
}
