// import * as React from 'react'
// import useSWR, { SWRResponse } from 'swr'

// import {} from '@interfaces/{{ camelCase endpointName }}'
import { fetcher } from '@lib/fetcher'

type {{ properCase hookName }} = () => {}

export const {{ camelCase hookName }}: {{ properCase hookName }} = () => {
  // return useSWR<unknown, unknown>('/api/{{ dashCase endpointName }}', fetcher)
  return
}
